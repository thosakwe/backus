(* Okay, this is more of something like a JS object literal, but... *)
(*digit = /0-9+/;
id = /[A-Za-z_][A-Za-z0-9_]*/;
pair = id, ":", expr;
object = "{", { pair }, "}";
expr = digit;
plus = "+";*)
arrow = "=", ">";